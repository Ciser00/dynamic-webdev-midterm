import React, {useEffect, useState, useMemo} from 'react';
import axios from "axios";
import quizData from "../containers/Home";
import Data from "../components/Data.json";
let num_right = 0;
//how to pass in and keep track of num right varible and how to disable the counter / buttons once an answer has been picked  Submit button ?
function Quiz() {     //why not work?
  return (
      switch (expression) {
        case expression:

          break;
        default:
      }

  )
}

export default Quiz;
